# `plet-file-destination`

Category: **Destination**. Connector: `storage`. Behavior: `dest_storage`.

Build: `docker build -f pipelets/plet-file-destination/Dockerfile -t dashflow/plet-file-destination:local pipelets`
